DEPLOYMENT NOTES:

- Update robots.txt sitemap URL from localhost:3000 to production domain
- Set NEXT_PUBLIC_BASE_URL env var for production
- Sitemap auto-generates from MDX pages in /content

DEVELOPMENT SETUP:

- Pre-commit hooks: Prettier + ESLint run automatically on commit
- Code formatting: Prettier (single quotes, no semicolons, 2-space tabs)
- Linting: ESLint with React/Next.js rules + accessibility checks
- Both flat config (eslint.config.js) and legacy (.eslintrc.js) for IDE support
- VSCode: Flat config enabled in .vscode/settings.json

BUILD & DEV:

- Dev server: pnpm dev (uses Turbopack)
- Lint: pnpm lint / pnpm lint:fix
- Build artifacts (.next/) ignored by git
- Pagefind search runs on postbuild


SCATTERED:

- Need to update eslint config for _meta.js files and new components
- Auto-lint potentially not running